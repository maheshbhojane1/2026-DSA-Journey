//  Some Method: Some method is used to check if at least one element in the array satisfies the provided testing function. It returns true if at least one element passes the test, otherwise it returns false.

let arr = [22, 33, 44, 55, 66, 145];

let result = arr.some(x => x > 100);


console.log(result);


